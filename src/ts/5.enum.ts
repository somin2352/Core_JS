// enumerable type
// 찐상수
enum Direction {
  UP = 3,
  DOWN,
  LEFT,
  RIGHT,
}

Direction.UP; //0

const direction = {
  up: Direction.UP,
  down: Direction.DOWN,
  left: Direction.LEFT,
  right: Direction.RIGHT,
};
