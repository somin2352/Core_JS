/* array */

// 숫자만 담는 배열
let numArr: number[] = [1, 2, 3];

// 문자만 담는 배열
let strArr: string[] = ["a", "b", "c"];

//다양한 타입을 포함할 수 있는 배열
let multiArr: (string | number | boolean)[] = ["hi", true];

// 숫자만 담을 수 있는 배열 (tuple 정의)
let matrix: [number, number, number][] = [
  [1, 2, 3],
  [4, 5, 6],
  [7, 8, 9],
];
